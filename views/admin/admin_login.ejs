<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Login Page</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: url('https://images.unsplash.com/photo-1655298801089-56ab67172c0e?q=80&w=2127&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D') no-repeat center center/cover;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      font-family: Arial, sans-serif;
    }
    .login-card {
      background: rgba(255, 255, 255, 0.2); /* Transparent look */
      padding: 2.5rem;
      border-radius: 15px;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
      width: 100%;
      max-width: 400px;
      backdrop-filter: blur(15px); /* Glass effect */
      border: 1px solid rgba(255, 255, 255, 0.3);
    }
    .login-card h2 {
      font-weight: bold;
      color: white;
      text-shadow: 0 0 5px rgba(0,0,0,0.5);
    }
    .form-label {
      color: white;
      font-weight: 500;
    }
    .form-control {
      border-radius: 10px;
      padding: 0.7rem;
      background: rgba(255, 255, 255, 0.3);
      border: none;
      color: white;
    }
    .form-control::placeholder {
      color: rgba(255, 255, 255, 0.7);
    }
    .form-control:focus {
      outline: none;
      box-shadow: 0 0 8px rgba(255, 255, 255, 0.6);
    }
    .btn-primary {
      border-radius: 10px;
      padding: 0.7rem;
      font-size: 1rem;
      font-weight: bold;
      background: rgba(0, 123, 255, 0.8);
      border: none;
      transition: background 0.3s;
    }
    .btn-primary:hover {
      background: rgba(0, 86, 179, 0.9);
    }
  </style>
</head>
<body>

  <div class="login-card">
    <h2 class="text-center mb-4"> Login</h2>
    <form action="/admin/login" method="post">
      <div class="mb-3">
        <label for="email" class="form-label">Email address</label>
        <input type="email" class="form-control" id="email" name="email" placeholder="Enter email" required>
      </div>
      <div class="mb-3">
        <label for="password" class="form-label">Password</label>
        <input type="password" class="form-control" id="password" name="password" placeholder="Enter password" required>
      </div>
      <button type="submit" class="btn btn-primary w-100">Login</button>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
